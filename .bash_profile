#!/bin/bash
# ~/.bash_profile: executed by bash for login shells.

# Source .profile for shared login shell configuration - NEW
[ -f ~/.profile ] && source ~/.profile

# Source .bashrc for interactive bash shells - STANDARD
[ -f ~/.bashrc ] && source ~/.bashrc
